@model Prog2bPOEPart2.Models.Document

@{
    ViewData["Title"] = "Delete Document";
}

<!-- Main container for the page -->
<div class="container my-5">
    <!-- Page Title -->
    <h2 class="text-center text-danger mb-4">Delete Document</h2>
    <!-- Warning Message -->
    <p class="text-center mb-4" style="color: #555;">
        Are you sure you want to delete this document? This action cannot be undone.
    </p>

    <!-- Card container for document details and actions -->
    <div class="card shadow-sm border-light" style="border-radius: 0;">
        <div class="card-body">
            <!-- Section title -->
            <h4 class="mb-4">Document Details</h4>
            <hr />
            <!-- Displaying document details in a definition list -->
            <dl class="row">
                <!-- File Name -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.FileName)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.FileName)
                </dd>

                <!-- File Type -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.FileType)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.FileType)
                </dd>

                <!-- File Path -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.FilePath)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.FilePath)
                </dd>

                <!-- File Size -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.FileSize)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.FileSize)
                </dd>

                <!-- Uploaded Date -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.UploadedDate)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.UploadedDate)
                </dd>

                <!-- Associated Claim ID -->
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Claim)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Claim.ClaimId)
                </dd>
            </dl>

            <!-- Action Buttons -->
            <div class="mt-4 text-center">
                <!-- Form to confirm deletion -->
                <form asp-action="Delete">
                    <!-- Hidden field to pass the document ID to the controller -->
                    <input type="hidden" asp-for="DocumentId" />
                    <!-- Delete button -->
                    <button type="submit" class="btn btn-danger btn-lg" style="border-radius: 20px;">Delete</button>
                </form>

                <!-- Button to navigate back to the document list -->
                <a asp-action="Index" class="btn btn-secondary btn-lg mt-2" style="border-radius: 20px;">Back to List</a>
            </div>
        </div>
    </div>
</div>
